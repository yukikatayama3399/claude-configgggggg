# 週次アタックリスト作成 ルーティン化メモ

> ステータス: **Routine登録済み（毎週木曜 09:00 JST 発火）**。cron: `0 0 * * 4`（UTC）。
> 発火時は新規セッションを立ち上げ、両シートの**記入案（下書き）を作成・提示**する。
> **自動では書き込まない**。ユーザーの承認後にのみ gog で記入する（下書き→確認型）。

## 発火タイミング
- **毎週木曜 09:00 JST**（= 木曜 00:00 UTC / cron `0 0 * * 4`）。
- その週（月曜始まり）の「MMDD週」タブ＋activityの当該週列の**記入案を作る**。
- 発火時はプッシュ通知で「下書きできました」を知らせる → ユーザーがレビュー → 承認後に書き込み。
- 安全ガード: ヨミ管理の対象週タブが既にデータで埋まっている場合は**上書き提案しない**（手動修正の保護）。

## 対象シート
### ① ヨミ管理 / 提案ステータス
`https://docs.google.com/spreadsheets/d/1KbOQJCgxaaAzTcQPmOgTjgYl8bCzZrOGyXhKUnK_ESs/edit`
- 当該週タブ（例 `0727週`）に記入。タブは事前作成されている前提。
- 列: A クライアント名 / B 担当 / C ステータス / D Next Action / E In or Out / F 議事録 / G "MMDD週"進捗メモ / H (空) / I 失注理由

### ② activity / HAWKビジネスプラン
`https://docs.google.com/spreadsheets/d/1S5EB3iEhx3P97_YE6Yx8Kxm_Fwao2Xo_pIpxCaqSB8k/edit`
- `Activity` タブの**「片山」ブロック**（11行目ヘッダー: A=片山, B=カテゴリ, C以降=各週）。
- 当該週の列を見つける（ヘッダーの "MMDD週" と一致。7/20週=H, 7/27週=I, 以降1列ずつ右）。
- 記入行:
  - 初回接触: 13=コールドリスト(info@) / 14=休眠・インバウンド / 15=紹介
  - 商談・デモ実施: 17=コールドリスト / 18=休眠・インバウンド / 19=紹介

## 記入ルール（ユーザー修正から確定）
1. **前週タブを参考**に案件を引き継ぐ。議事録は `=HYPERLINK("<docUrl>","<議事録名>")` で貼る（前週F列のリンク流用）。
2. 含めるもの:
   - **導入済/オンボーディング中** → ステータス `1_導入済`
   - **今週の新規アポ・商談**
   - **商談中/検討中** → `3_商談中` / `4_商談中（先方検討中）` / `5_開始`
   - **メールで追いかけた過去商談社** → 最下部（優先度・重要度低）
   - **失注**
3. **追いかけ中でも現状案件がない会社は「失注」に倒す**（Next Actionに「継続追いかけ。現状案件なし」等）。進捗メモは `【追いかけ】…` を付ける。
4. **失注は失注理由(I列)を必ず記入**（例: 案件不足／採算合わず／解約理由など具体的に）。
5. 動いていない/古すぎる案件は載せない（例: 電通北海道は7/20週で除外）。
6. In or Out はシンプルに（例: 知り合い/代理販売コア → `紹介`）。
7. 情報源: 前週タブ / Googleカレンダー（今週の商談・オンボーディング予定）/ Drive議事録 / Slack。

## activity 数字の数え方（確定）
- **その週に新規実施した「初回接触」「商談・デモ」をリード元で分類してカウント**。
- **オンボーディング**（導入済の実装作業）や**過去商談の追いかけ**はカウントしない（初回接触にも商談にも含めない）。
- リード元分類:
  - コールドリスト(info@) = info@ への新規アウトバウンド
  - 休眠・インバウンド = ad_info 等インバウンド／休眠の再開
  - 紹介 = 社内／知人／パートナー紹介
- 例（7/20週・片山）: 初回接触 0/0/0、商談 コールド0・休眠1（アドブレーン=ad_info）・紹介2（サイバーバズ=KAN紹介, ながのアドビューロ=DSPパートナー）。

## 記入フロー（下書き→確認型）
1. 発火後、両シートの現状と前週タブ・カレンダー等を調べ、**記入案を作成**する。
2. ヨミ管理は表形式、activityは数字の内訳を**チャットに提示**（推測箇所は明示）。
3. **この時点では書き込まない**。ユーザーの「OK／修正指示」を待つ。
4. 承認後に gog で記入し、記入結果を報告する。

## 実行手段（gog）
- 読み: `gog --account yuki.katayama@fout.jp -p sheets get <ID> "<タブ>!A1:I50"`
- 書き: `gog --account yuki.katayama@fout.jp sheets update <ID> "<タブ>!A2:I20" --values-json '<2D配列>' --input USER_ENTERED`
- カレンダー: `gog --account yuki.katayama@fout.jp -p calendar events --from <月曜> --to <翌月曜>`
- 行/列の対応は raw grid（`sheets raw <ID> --include-grid-data`）で確定させる。

---
_2026-07-22 作成 / 同日 ルーティン登録・ユーザー修正反映。7/20週タブ・activity片山H列は記入済み。_
