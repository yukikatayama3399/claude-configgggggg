# Runbook: HAWK フェーズ2 宛名（姓名逆順）修正 → 再生成 → 検証 → 送信

- 対象: ウェビナー_新規リスト フェーズ2 下書き **950件**（フェーズ2 進捗 1900/4414）
- 状態: **送信見送り（2026-07-10）** — 宛名が全件「名 姓」の逆順のため中止中
- 最終更新: 2026-07-10

> ⚠️ このrunbookの「STEP 5 送信」は、**STEP 4 の検証がすべてOKになるまで実行しないこと。**
> `~/hawk_send/PAUSED` は消さないこと（自動送信との二重送信防止）。

---

## 1. 問題の概要

- **事象**: 下書きの宛名が全件「名 姓 様」の逆順になっている。
  - 例: `早希 水谷 様` → 正: `水谷 早希 様`
  - 例: `勲 桑原 様` → 正: `桑原 勲 様` / `崇史 大植 様` → 正: `大植 崇史 様`
- **原因**: 差し込みの結合順ミス。Salesforce の `FirstName` / `LastName` を**欧米式（名+姓）**で結合していた。
  - 誤: `FirstName + " " + LastName`（名 姓）
  - 正: `LastName + " " + FirstName`（姓 名）
- **影響範囲**: フェーズ2の下書き 950件すべて（宛名行のみ。本文・署名・リンク類は別問題ではない）。

---

## 2. 修正方針

下書き**生成スクリプト**の氏名結合ロジックを「姓+名」に直し、**950件を再生成**する。
既存の逆順下書きは破棄（または上書き再生成）し、送信は検証OK後に限る。

参考（結合ロジックのイメージ / 実ファイル・関数名は要確認）:

```python
# 誤（欧米式・名 姓）
name = f"{first_name} {last_name}"

# 正（日本式・姓 名）
name = f"{last_name} {first_name}"
salutation = f"{name} 様"
```

> 実際の該当箇所（生成スクリプトのファイル名・関数）はMac側で確認して修正すること。
> 本リポジトリには生成スクリプトは含まれていない（Mac側 `~/hawk_send/` 配下）。

---

## 3. 前提・関連ファイル（Mac側）

| 項目 | パス / 値 |
|---|---|
| 送信スクリプト | `~/hawk_send/send_manual.py` |
| 送信対象の下書きIDリスト | `~/hawk_send/phase2_draft_ids.txt` |
| 二重送信防止フラグ | `~/hawk_send/PAUSED`（**消さない**） |
| 送信実績の記入先 | スプレッドシート「ウェビナー_新規」P列（送信実施日） |
| CC | `hawk-sales@fout.jp` |

---

## STEP 1. 生成スクリプトの結合順を修正

1. 下書き生成スクリプト内の氏名結合箇所を `LastName + " " + FirstName`（姓+名）に修正。
2. 姓・名が片方欠損しているレコードの挙動を確認（余分な空白・「 様」だけにならないか）。
3. 変更をローカルにコミット等で記録（可能なら）。

## STEP 2. 950件を再生成

1. 修正後スクリプトで950件の下書きを再生成する。
2. 再生成後、`~/hawk_send/phase2_draft_ids.txt` が**新しい下書きID**に更新されていることを確認。
   - 旧IDのまま送ると逆順下書きを送ってしまうため必ず確認。
3. 旧（逆順）の下書きが残っている場合は破棄する。

## STEP 3. 送信前チェックリスト（機械的確認）

再生成後、以下をまとめて確認する。

- [ ] `phase2_draft_ids.txt` の件数が **950** 行ある
- [ ] 宛名が全件 **「姓 名 様」** の順（下記STEP 4のサンプル確認）
- [ ] CC = `hawk-sales@fout.jp` が全件に入っている
- [ ] PDF/資料が**新URL**になっている（旧URLが残っていない）
- [ ] 予約リンクが入っている
- [ ] 旧URLがどこにも残っていない

## STEP 4. 宛名の抜き取り検証（必須）

逆順バグの再発を防ぐため、宛名順を必ず目視/機械で確認する。

1. 既知の逆順サンプルが**正しい順**になっているか確認:
   - `早希 水谷 様` → `水谷 早希 様`
   - `勲 桑原 様` → `桑原 勲 様`
   - `崇史 大植 様` → `大植 崇史 様`
   - `孝介 土井 様` → `土井 孝介 様`
   - `雅史 吉田 様` → `吉田 雅史 様`
2. ランダムに20〜30件抜き取り、Salesforce の `LastName + FirstName` と宛名が一致するか照合。
3. できれば「Salesforce上の姓が宛名の先頭に来ているか」を機械チェック（先頭トークン == LastName）。

**ここまで全部OKで初めてSTEP 5へ進む。**

## STEP 5. 送信（検証OK後のみ）

> STEP 4 の検証がすべてOKであることを確認してから実行すること。

```bash
python3 ~/hawk_send/send_manual.py ~/hawk_send/phase2_draft_ids.txt 950 20 100 60
```

**パラメータの意味:**

| 引数 | 値 | 意味 |
|---|---|---|
| 1 | `~/hawk_send/phase2_draft_ids.txt` | 送信対象の下書きIDリスト |
| 2 | `950` | 送信件数 |
| 3 | `20` | 送信間隔（秒） |
| 4 | `100` | 100通ごとに休憩 |
| 5 | `60` | 休憩時間（秒） |

- 想定所要時間: 約5.4時間（例: 10:30開始 → 16時頃完了）
- **Ctrl+C で中断可**。再実行すれば続きから（送信済みは自動スキップ）。
- `~/hawk_send/PAUSED` は消さないこと。

## STEP 6. 送信後

- スプレッドシート「ウェビナー_新規」**P列（送信実施日）**に日付を記入。
- 送信件数・エラー件数を確認し、失敗分があれば再実行（済み分は自動スキップ）。

---

## ロールバック / 中断時

- 送信途中の中断は Ctrl+C。再実行で続きから再開（送信済みスキップ）。
- 宛名ミス等に途中で気づいた場合は即 Ctrl+C し、`PAUSED` は残したまま原因調査 → STEP 1 へ戻る。
